#include <stdio.h>
/**
 * Description: main - prints out a string to stdout.
 * Return: 0 id success.
 */
int main(void)
{
char jeje[] = "with proper grammar, but the outcome is a piece of art, ";
 printf("%s",jeje);
return (0);
}
