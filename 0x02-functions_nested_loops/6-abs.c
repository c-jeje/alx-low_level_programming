#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: integer number
 * Return: returns an integer value
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		n = (n / 1);
	}
	else
	{
		n = ((n / 1) * -1);
	}
	return (n);
}
